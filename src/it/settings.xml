<?xml version="1.0" encoding="UTF-8"?>


<settings>
  <profiles>
    <profile>
      <id>it-repo</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <properties>
        <!--
        keystore generated via:
        keytool -genkey -alias test-01 -keypass key-passwd -keystore keystore -storepass store-passwd -validity 4096 -dname "cn=www.example.com, ou=None, L=Seattle, ST=Washington, o=ExampleOrg, c=US"
        -->
        <keystore.path>@project.basedir@/src/it/keystore</keystore.path>
        <keystore.storepass>store-passwd</keystore.storepass>

        <jarsigner.keypass>key-passwd</jarsigner.keypass>

        <!-- The following doesn't work as there is no way to map the property name into the POJO -->
        <jarsigner.signature.algorithm>RSAwithSHA256</jarsigner.signature.algorithm>
      </properties>
      <repositories>
        <repository>
          <id>local.central</id>
          <url>@localRepositoryUrl@</url>
          <releases>
            <enabled>true</enabled>
          </releases>
          <snapshots>
            <enabled>true</enabled>
          </snapshots>
        </repository>
      </repositories>
      <pluginRepositories>
        <pluginRepository>
          <id>local.central</id>
          <url>@localRepositoryUrl@</url>
          <releases>
            <enabled>true</enabled>
          </releases>
          <snapshots>
            <enabled>true</enabled>
          </snapshots>
        </pluginRepository>
      </pluginRepositories>
    </profile>
  </profiles>
</settings>
